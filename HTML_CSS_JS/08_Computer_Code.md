<h2>Question 1</h2>
<p><strong>Question:</strong> Write a complete HTML paragraph that explains a Python function using the correct element for inline code.</p>
<p><strong>Answer:</strong></p>
<p>The Python function <code>len()</code> is used to find the length of a sequence.</p>
<h2>Question 2</h2>
<p><strong>Question:</strong> Create a multi-line Python function using the correct preformatted element.</p>
<p><strong>Answer:</strong></p>
<pre>def factorial(n):
    if n == 0:
        return 1
    else:
        return n * factorial(n - 1)</pre>
<h2>Question 3</h2>
<p><strong>Question:</strong> Write an HTML sentence that shows a keyboard shortcut.</p>
<p><strong>Answer:</strong></p>
<p>Press <kbd>Ctrl + C</kbd> to copy the selected text.</p>
<h2>Question 4</h2>
<p><strong>Question:</strong> Write an HTML paragraph that displays sample program output.</p>
<p><strong>Answer:</strong></p>
<p>The program produces the output <samp>Hello, World!</samp> when it runs successfully.</p>
<h2>Question 5</h2>
<p><strong>Question:</strong> Write an HTML sentence that contains a mathematical or programming variable.</p>
<p><strong>Answer:</strong></p>
<p>The formula is F = <var>m</var>a, where <var>m</var> represents mass.</p>
<h2>Question 6</h2>
<p><strong>Question:</strong> Create a styled multi-line code block.</p>
<p><strong>Answer:</strong></p>
<style>
pre.code-block {
    background-color: #1e1e1e;
    color: white;
    padding: 20px;
    border-radius: 10px;
    font-family: monospace;
}
</style>
<pre class="code-block">for i in range(5):
    print(i)</pre>
<h2>Question 7</h2>
<p><strong>Question:</strong> Write HTML that demonstrates the difference between inline code and preformatted code.</p>
<p><strong>Answer:</strong></p>
<p>The CSS property <code>color: blue;</code> changes the text color.</p>
<pre>git add .
git commit -m "Initial commit"
git push origin main</pre>
<h2>Question 8</h2>
<p><strong>Question:</strong> Create a paragraph that shows both a keyboard command and its sample output.</p>
<p><strong>Answer:</strong></p>
<p>Type <kbd>pwd</kbd> in the terminal to display the current directory, such as <samp>/home/user/project</samp>.</p>
<h2>Question 9</h2>
<p><strong>Question:</strong> Write a complete HTML snippet that styles the keyboard input element.</p>
<p><strong>Answer:</strong></p>
<style>
kbd {
    background-color: #f2f2f2;
    border: 1px solid #999;
    padding: 4px 8px;
    border-radius: 5px;
    font-family: monospace;
}
</style>
<p>Press <kbd>Enter</kbd> to submit the form.</p>
<h2>Question 10</h2>
<p><strong>Question:</strong> Write HTML that correctly represents a Bash condition as inline code.</p>
<p><strong>Answer:</strong></p>
<p>The Bash command <code>test -f filename</code> checks whether a file exists.</p>
<h2>Question 11</h2>
<p><strong>Question:</strong> Create a preformatted block that displays simple ASCII art.</p>
<p><strong>Answer:</strong></p>
<pre>+----------+
|   Hello  |
|   World  |
+----------+</pre>
<h2>Question 12</h2>
<p><strong>Question:</strong> Write a paragraph that contains a programming variable and an assignment.</p>
<p><strong>Answer:</strong></p>
<p>The variable <var>count</var> is assigned the value <code>count = 10</code>.</p>
<h2>Question 13</h2>
<p><strong>Question:</strong> Write a complete HTML page section that demonstrates three different elements.</p>
<p><strong>Answer:</strong></p>
<p>The command <code>git status</code> displays the current Git status.</p>
<p>Press <kbd>Ctrl + S</kbd> to save the file.</p>
<p>The program displays <samp>File saved successfully.</samp> as the output.</p>
<h2>Question 14</h2>
<p><strong>Question:</strong> Create a styled keyboard input and a styled preformatted code block on the same page.</p>
<p><strong>Answer:</strong></p>
<style>
kbd.styled-key {
    background-color: #f5f5f5;
    border: 1px solid #999;
    padding: 5px 9px;
    border-radius: 5px;
    font-family: monospace;
}
pre.styled-code {
    background-color: #1e1e1e;
    color: white;
    padding: 20px;
    border-radius: 10px;
    font-family: monospace;
}
</style>
<p>Press <kbd class="styled-key">Ctrl + S</kbd> to save the file.</p>
<pre class="styled-code">print("Hello, World!")
print("Welcome to Python!")</pre>
<h2>Question 15</h2>
<p><strong>Question:</strong> Write a complete, valid HTML5 example that follows best practices.</p>
<p><strong>Answer:</strong></p>
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>HTML Code Elements</title>
    <style>
        pre {
            background-color: #1e1e1e;
            color: white;
            padding: 15px;
            border-radius: 8px;
            font-family: monospace;
        }
        kbd {
            background-color: #f2f2f2;
            border: 1px solid #999;
            padding: 4px 8px;
            border-radius: 5px;
            font-family: monospace;
        }
    </style>
</head>
<body>
    <p>The CSS property <code>color: red;</code> changes the text color.</p>
    <pre>if x &gt; 10:
    print("Number is greater than 10")
else:
    print("Number is 10 or less")</pre>
    <p>Press <kbd>Ctrl + S</kbd> to save the document.</p>
    <p>The program displays <samp>Error: File not found.</samp> if the file is missing.</p>
    <p>The variable <var>x</var> stores a numerical value.</p>
</body>
</html>
