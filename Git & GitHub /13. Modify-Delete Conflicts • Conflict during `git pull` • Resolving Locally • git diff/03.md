Assignment 3
Combine Both + Use git diff + Reflection

Goal: Practice both types of conflicts and use git diff commands properly.

Part A – Practical

Create a situation that produces a Modify/Delete conflict (different file from Assignment 2, e.g. profile.txt).
Resolve it locally following the same steps taught in class.
Separately, create a pull conflict on main with a file named summary.txt.
While resolving, run these commands and observe the output:
git diff
git diff --staged
git diff --cached
Part B – Reflection
Write answer in your own words in your notebook:

What is a Modify/Delete conflict? Give one real-world example.
Why does GitHub sometimes say “This conflict is too complex to resolve in the web editor”?
What is the difference between git diff and git diff --staged?
After resolving any conflict, what three commands do you almost always run?
Submit:

Links / screenshots of both conflicts you resolved
Screenshots of the three git diff commands output
Final repository link
Photos of written answers
