Assignment 3 – feature/navbar Independent Full Cycle (Mandatory)
Goal: Independently complete one more full PR lifecycle.

Create feature/navbar branch from updated main.
Create navbar.html with a heading and 3–4 lines describing what a navigation bar contains (Home, About, Contact, etc.).
Commit, push, open PR with a clear title and description.
Merge the PR on GitHub.
Delete remote branch.
Update local main using:
git fetch origin main
git merge origin/main
Delete local branch with git branch -d feature/navbar.
Run git log --oneline -10 and take a screenshot showing the merge commits from the features you completed.
Submit: Merged PR link + screenshot of git log --oneline.
